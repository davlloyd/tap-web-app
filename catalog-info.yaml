apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: tap-web-app
  description: Demo app for TAP
  tags:
    - app-accelerator
    - java
    - spring
    - web
    - tanzu
  annotations:
    'backstage.io/kubernetes-label-selector': 'app.kubernetes.io/part-of=tap-web-app'
spec:
  type: service
  lifecycle: experimental
  owner: default-team
